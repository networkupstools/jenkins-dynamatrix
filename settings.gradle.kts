pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

/*
plugins {
    // ex "build-scan"
    id "com.gradle.enterprise" version "3.7.2"
}
*/

rootProject.name = "jenkins-dynamatrix"
